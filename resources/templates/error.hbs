{{#*inline "content"}}
    <div class="flex justify-center pt-12">
        <div class="mb-12 max-w-sm">
            <h3 class="text-5xl font-bold text-center">{{status_code}}</h3>
            <p class="p-4 text-lg">
                {{error}}
            </p>
        </div>
    </div>
{{/inline}}
{{~> layouts/app_layout ~}}
